# Web framework
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
websockets>=14.0,<15.0

# Configuration
pydantic-settings>=2.7,<3.0
pyyaml>=6.0,<7.0

# VAD
silero-vad>=5.1,<6.0

# STT
transformers>=4.40,<5.0
torch>=2.0
accelerate>=0.30

# TTS (Chatterbox French)
chatterbox-tts
huggingface-hub
safetensors

# LLM client (async HTTP + SSE parsing)
httpx>=0.28,<1.0
httpx-sse>=0.4,<1.0

# Grammar correction
language-tool-python>=3.0,<4.0

# Audio utilities
numpy>=2.0,<3.0

# Dev/test
pytest>=8.0,<9.0
pytest-asyncio>=0.24,<1.0
